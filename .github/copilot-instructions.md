# Repository instructions

Read and follow `/AGENTS.md`. Treat `/docs/INTERFACES.md` as the canonical public API and `/docs/DECISIONS.md` as the frozen design record. Work only within the assigned `/tasks/*.md` scope. Do not change units, array shapes, event fields, or public signatures without an explicit recorded team decision. Human review and the checks listed in `AGENTS.md` are required.
